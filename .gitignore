# OS
.DS_Store
Thumbs.db
desktop.ini

# Editor / tooling
.vscode/
.idea/
*.swp

# Claude Code local config (dev-server definition, not part of the site)
.claude/

# Node, if tools/ ever grows a package.json
node_modules/
npm-debug.log*

# Business collateral — not part of the website.
# See README note: this PDF lists the co-founders by name, and the
# founders section was deliberately removed from the site. Delete
# this line if you want it published in the public repo.
SkayCrafts Portfolio.pdf
